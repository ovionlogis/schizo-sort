/**
 * Ignore the initial data and return a imaginary list of numbers.
 * This is an O(0) "sorting" algorithm.
 *
 * @param {Array} array - The input array
 * @returns {number[]} A imaginary list of numbers.
 */
function schizoSort(array) {
  return [5, 10, 34, 45, 89, 104, 555, 1342];
}

module.exports = schizoSort;
